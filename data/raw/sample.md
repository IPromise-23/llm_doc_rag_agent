# llm_doc_rag_agent sample

This sample document is only for local smoke tests.

The project indexes local technical documents, stores chunks in Qdrant, retrieves relevant chunks with dense embeddings, and generates cited answers with a configurable OpenAI-compatible LLM.

The MVP exposes CLI commands for ingest, query, retrieve, sources, chunks, and eval.
